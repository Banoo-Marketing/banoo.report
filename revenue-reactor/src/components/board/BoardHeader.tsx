// BoardHeader is no longer used — board/page.tsx renders its own header inline.
// Keeping file to avoid import errors during transition; can be deleted once all imports are removed.
export function BoardHeader() { return null }
